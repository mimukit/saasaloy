{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://saasaloy.dev/schemas/manifest.schema.json",
  "title": ".saasaloy/manifest.json — managed-file tracking",
  "description": "Records every file a module applied — copied source files and copied skill files — with the owning module and a content hash, so update/remove know exactly what to undo. Replaces in-file `// saasaloy:managed` markers (build spec §2.9 / §3.2). The base writes no manifest; it is created by the first `saasaloy add`.",
  "type": "object",
  "additionalProperties": false,
  "required": ["managed"],
  "properties": {
    "$schema": {
      "type": "string",
      "description": "Optional pointer to this schema for editor validation."
    },
    "managed": {
      "type": "object",
      "description": "Project-relative file path → the module that owns it and a content hash. On update the tool re-hashes the file: match → safe overwrite; drift → route to AI-merge.",
      "additionalProperties": {
        "type": "object",
        "additionalProperties": false,
        "required": ["module", "hash"],
        "properties": {
          "module": {
            "type": "string",
            "pattern": "^[a-z0-9][a-z0-9-]*$",
            "description": "Name of the module that applied this file."
          },
          "hash": {
            "type": "string",
            "pattern": "^[a-f0-9]{64}$",
            "description": "sha256 hex digest of the file's content as written."
          },
          "from": {
            "type": "string",
            "minLength": 1,
            "description": "Module-relative POSIX source path this file was copied from (e.g. `files/lib/email.ts`). `update` uses it to fetch the same file at the old and new commit SHAs. Optional — entries written before this field shipped fall back to re-deriving the target from the module descriptor."
          }
        }
      }
    },
    "links": {
      "type": "object",
      "description": "Symlink map (source path → link path): a module's skill files live under .agents/skills/<name>/ and `add` points a .claude/skills/<name> symlink at them, recorded here so `remove` can undo both (ADR 0015).",
      "additionalProperties": {
        "type": "string",
        "minLength": 1
      }
    },
    "removeWarnings": {
      "type": "object",
      "description": "Installed module name to removal warnings copied from its descriptor. The remover reads this map offline.",
      "propertyNames": {
        "pattern": "^[a-z0-9][a-z0-9-]*$"
      },
      "additionalProperties": {
        "type": "array",
        "items": {
          "type": "string",
          "minLength": 1
        }
      }
    },
    "patches": {
      "type": "array",
      "description": "Structural config patches actually applied to disk, so `remove` can warn which files it can't clean up on its own (reversal is a follow-up issue). Deduped on re-apply (structural equality) so `--force` stays idempotent.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["module", "file", "patch"],
        "properties": {
          "module": {
            "type": "string",
            "pattern": "^[a-z0-9][a-z0-9-]*$",
            "description": "Name of the module that applied this patch."
          },
          "file": {
            "type": "string",
            "minLength": 1,
            "description": "Project-relative POSIX path of the file that was patched."
          },
          "patch": {
            "type": "object",
            "required": ["file", "kind"],
            "properties": {
              "file": {
                "type": "string",
                "minLength": 1
              },
              "kind": {
                "type": "string",
                "enum": [
                  "wrangler-binding",
                  "package-json-dependency",
                  "package-json-script",
                  "plugin-array",
                  "const-array",
                  "chained-route"
                ],
                "description": "Which codemod applied this patch. Must list exactly the kinds registry-item.schema.json accepts — the applier records here whatever a descriptor authored, so a kind missing from this enum makes the next `loadManifest` refuse a manifest the tool itself wrote (#98). `schema.test.ts` asserts the two enums and the `Patch` union agree."
              }
            },
            "description": "The op as authored (kind + payload + file), re-applicable at execute time."
          }
        }
      }
    }
  }
}
