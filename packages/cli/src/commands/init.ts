import { logger } from "../lib/logger.js";

// Phase 0 (in progress): scaffold the base project — Astro landing + packages/ui
// + packages/config, the .agents/ layer, then run `sync`. Implemented next.
export async function runInit(_argv: string[]): Promise<number> {
  logger.error("`saasaloy init` is not implemented yet.");
  return 1;
}
