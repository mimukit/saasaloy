{
  "name": "saasaloy-cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Saasaloy CLI + module registry — the tool repo",
  "license": "MIT",
  "packageManager": "pnpm@11.14.0",
  "engines": {
    "node": ">=24.0.0",
    "pnpm": ">=11"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "cli": "node packages/cli/dist/index.js",
    "cli:dev": "pnpm --filter saasaloy dev",
    "play:init": "pnpm --filter saasaloy build && node packages/cli/dist/index.js init .dev/playground --force --no-install && cp scripts/saasaloy-shim.sh .dev/playground/saasaloy && chmod +x .dev/playground/saasaloy",
    "play:destroy": "rm -rf .dev/playground",
    "play:reset": "pnpm run play:destroy && pnpm run play:init",
    "play:watch": "node scripts/watch-template.mjs",
    "deps:check": "node scripts/update-deps.mjs --check",
    "deps:update": "node scripts/update-deps.mjs",
    "deps:verify": "pnpm run play:init && pnpm -C .dev/playground install && pnpm -C .dev/playground build && pnpm -C .dev/playground typecheck"
  },
  "devDependencies": {
    "turbo": "2.10.5",
    "typescript": "7.0.2"
  }
}
