# Provider implementations live here, one file per provider. `saasaloy add
# logger-console` drops its file in and appends it to the `providers` array in
# src/index.ts — no edit anywhere else. Writing your own? Export a factory
# returning a `LogProvider` and add it to that array by hand; see the
# saasaloy-logger skill. This folder is empty until a provider is installed.
