{
  "name": "@repo/db",
  "private": true,
  "type": "module",
  "//exports": "No patch kind edits an exports map, so entries a feature module fills are declared here up front. './tenant' and './tenant-column' are filled by the multitenant module (src/tenant.ts, src/tenant-column.ts); until it is installed the subpaths resolve to nothing and nothing imports them.",
  "exports": {
    "./client": "./src/client.ts",
    "./schema/*": "./src/schema/*.ts",
    "./repositories/*": "./src/repositories/*.ts",
    "./tenant": "./src/tenant.ts",
    "./tenant-column": "./src/tenant-column.ts"
  },
  "scripts": {
    "clean": "rimraf -g dist \"*.tsbuildinfo\"",
    "db:generate": "drizzle-kit generate",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "drizzle-orm": "0.45.2"
  },
  "devDependencies": {
    "@repo/tsconfig": "workspace:*",
    "drizzle-kit": "0.31.10",
    "rimraf": "6.1.3",
    "typescript": "7.0.2",
    "vite": "8.2.2"
  }
}
